/**
 * Authentication &amp; verification module (owned by P1).
 *
 * <p>Covers SRS FR-1 through FR-5: student/alumni registration, login,
 * password reset, and the pending-verification gate that keeps alumni
 * features locked until an admin approves the account.
 *
 * <p>Empty on Day 1 -- this package exists so the project's shape matches
 * the architecture from the start. The first real code lands here Day 4
 * (registration endpoint) per the team plan.
 */
package com.campusmentor.backend.auth;
